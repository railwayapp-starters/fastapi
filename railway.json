{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c 'celery -A celery_worker worker --loglevel=debug & hypercorn main:app --bind \"[::]:$PORT\" --log-level debug'"
  }
}
